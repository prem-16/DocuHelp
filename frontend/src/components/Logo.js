import React from 'react';
import './Logo.css';

const Logo = () => {
    return (
        <div className="logo-container">
            <span className="logo-icon">+</span>
            <span className="logo-text">DocuHelp</span>
        </div>
    );
};

export default Logo;
